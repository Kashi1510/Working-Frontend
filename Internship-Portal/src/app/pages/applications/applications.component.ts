import { Component} from '@angular/core';
// import { ApplicationService } from '../../services/application.service';
// import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-applications',
  imports: [],
  templateUrl: './applications.component.html',
  styleUrl: './applications.component.css'
})
export class ApplicationsComponent {
  // applications: any[] = [];
  // studentEmail: string = 'student@example.com'; // This should be dynamically set for the logged-in student

  // constructor(private applicationService: ApplicationService) {}

  // ngOnInit() {
  //   // Ensure you send the email as part of the request
  //   this.applicationService.getApplications(this.studentEmail).subscribe(data => {
  //     this.applications = data;
  //   });
  // }

}
